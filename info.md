This is a simple-ish implementation of a space invaders like game for the zx spectrum.

We have the fuse emulator included in the project to run the code.

In orger to load the code into FUSE, we need to turn it into a TAP file, which we can use "bas2tap.exe" to achieve